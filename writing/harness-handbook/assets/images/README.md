# Images

Reserved for raster images (screenshots, photos). The blog currently uses only
vector graphics (inline SVG + files in `../illustrations/`), so this folder is
intentionally empty. Add raster assets here and reference them with relative
paths from `index.html`.
